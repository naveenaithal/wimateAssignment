server.js
router
controller
service


router
    index.js
    const employeeRoute=require('/empRoute')
    employeeRoute.getPersonalDetails(router);

    empRoute
    const employee=require('/empController')
    exports.getPersonalDetails(router)=>{
        return router.get('/getDetails',(req,res)=>{
            return employee.getPersonalDetails(req,res)
        })
    }

    empController
    const empService=require('/empService')
    exports.getPersonalDetails=(req,res)=>handler(req,res, async()=>{
                return empService.get(req);
    })


    empService
    exports.get(req)=>{
        ...
        ...
        return {result:"Data"}
     }


    <!-- helper functions -->
    const handler=(req,res,operations,checkAuth=true)=>{
        ...some validations
        result=operations()
        ... validation function responseHandler()
        res.send(result)
         res.end();
    }



